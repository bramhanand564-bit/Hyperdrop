# Hyperdrop Core

Core services: mind (decision/behavior), research (search/read/evidence), verification (provenance/conflicts), memory (persistent compressed knowledge), agent (tools/permissions/checkpoints), learning (reflection), runtime (lifecycle/config).

The design keeps local state small so a lightweight phone can orchestrate heavier external tools.